#!/bin/sh

set -e

npm install
export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"
export HUBOT_VERSION="$(git rev-parse --verify HEAD || echo 'unknown')"
exec node_modules/.bin/hubot --name "hubot" "$@"
